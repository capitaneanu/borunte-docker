# Borunte Development Environment in a Docker Container

This Docker image set's up everything for working on the Borunte Machinekit + ROS project.

Build:

```bash
./docker-dev.sh -b
```

User:

```bash
./docker-dev.sh
```
